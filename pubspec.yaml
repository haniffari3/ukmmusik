name: musiku_app
description: Aplikasi Flutter untuk penjualan musik online
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  google_fonts: ^6.1.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/lologo.png
dev_dependencies:
  flutter_test:
    sdk: flutter
